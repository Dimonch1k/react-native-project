import React from 'react'
import { Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

export default function EventsScreen() {
	return (
		<SafeAreaView>
			<Text className='text-xl'>Events Screen</Text>
		</SafeAreaView>
	)
}
